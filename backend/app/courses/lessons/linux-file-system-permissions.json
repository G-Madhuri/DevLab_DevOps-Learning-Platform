{
  "slug": "linux-file-system-permissions",
  "title": "Linux File System & Permissions",
  "overview": {
    "introduction": "In Linux, every file and directory has an owner and a set of permissions that control who can read, write, or execute it. Getting permissions wrong is one of the most common causes of deployment failures and security vulnerabilities.",
    "why_it_matters": "Misconfigured file permissions are a leading cause of security breaches. A world-writable config file or an SSH key readable by other users can compromise an entire server.",
    "industry_usage": "Used by system administrators, DevOps engineers, and security teams to control access to application config files, SSH keys, deployment scripts, and database credentials.",
    "what_you_will_build": "You will configure file permissions for a simulated deployment: set correct permissions on config files, scripts, and SSH keys — the way it's done in real production environments.",
    "prerequisites": "Linux Command Line Basics — you should know ls, cd, pwd, touch, and mkdir.",
    "estimated_time": "80 mins",
    "difficulty": "Beginner",
    "learning_outcomes": [
      "Read the permission string (e.g., -rwxr-xr--) from ls -l output.",
      "Use chmod with symbolic (u+x) and numeric (755) modes.",
      "Change file ownership using chown and chgrp.",
      "Understand why SSH keys must be permission 600 and directories 700.",
      "Apply correct permissions for web server files, scripts, and configs."
    ]
  },
  "key_concepts": [
    {
      "title": "Understanding the Permission String",
      "definition": "Every file has a 10-character permission string that defines who can read, write, or execute it — shown in ls -l output.",
      "why_important": [
        "Determines who can access sensitive files like private keys and passwords.",
        "Prevents unauthorized users from modifying scripts or configs.",
        "SSH refuses to connect if your private key has loose permissions.",
        "Web servers and apps fail to start if config files aren't readable."
      ],
      "explanation": "When you run `ls -l`, the first column is the permission string:\n\n  -rwxr-xr--\n  ↑ ↑↑↑ ↑↑↑ ↑↑↑\n  │ │││ │││ └── Others: r-- (read only)\n  │ │││ └────── Group: r-x (read + execute)\n  │ └────────── Owner: rwx (read + write + execute)\n  └──────────── File type: - (file) or d (directory)\n\nThree permission levels:\n\n| Who | Symbol | Description |\n|:---|:---|:---|\n| Owner (u) | user | The file creator/owner |\n| Group (g) | group | Users in the file's group |\n| Others (o) | other | Everyone else |\n\nThree permission types:\n\n| Permission | Symbol | Meaning on file | Meaning on directory |\n|:---|:---|:---|:---|\n| Read | r | View file content | List directory contents |\n| Write | w | Modify file | Create/delete files inside |\n| Execute | x | Run as program | Enter directory with cd |",
      "commands": [
        {
          "name": "ls -l",
          "purpose": "Display files with full permission strings, ownership, and size.",
          "syntax": "ls -l [path]",
          "parameters": "-l: long format. path: optional directory to inspect.",
          "example": "ls -l",
          "expected_output": "-rw-r--r-- 1 student student   45 Jan 12 config.env\n-rwxr-xr-x 1 student student  120 Jan 12 deploy.sh\ndrwxr-xr-x 2 student student 4096 Jan 12 secrets/",
          "output_explanation": "config.env is readable by all but writable only by owner. deploy.sh is executable. secrets/ is a directory (d prefix).",
          "when_to_use": "Always run ls -l before modifying permissions to see the current state.",
          "real_world_use": "Verify .env file permissions during a security audit of a production server."
        }
      ],
      "pro_tip": "Add `stat filename` to see even more detail including the numeric permission value.",
      "best_practices": "Run ls -la regularly in config directories to audit access levels on sensitive files.",
      "common_mistakes": "Confusing the owner's permissions with the file type character. The first character (- or d) is NOT a permission.",
      "real_world_example": "A security engineer audits /etc/ssh/sshd_config and checks that it is -rw------- (owner-only read/write) to prevent other users from reading SSH configuration secrets.",
      "remember": "d = directory, - = regular file, l = symbolic link. These are the most common file type characters.",
      "summary": "The 10-character permission string tells you everything about file access. Read it from left to right: type, owner, group, others."
    },
    {
      "title": "Changing Permissions with chmod",
      "definition": "chmod (change mode) modifies the read/write/execute permissions on files and directories.",
      "why_important": [
        "Scripts must be executable (+x) before you can run them.",
        "Config files should not be world-readable if they contain passwords.",
        "SSH private keys must be 600 or SSH will refuse to use them.",
        "Web server directories typically need 755 for the server to serve files."
      ],
      "explanation": "Two ways to use chmod:\n\n**1. Symbolic Mode** (human-readable):\n```\nchmod [who][+/-/=][permission] file\n```\n\n| Symbol | Who |\n|:---|:---|\n| u | User (owner) |\n| g | Group |\n| o | Others |\n| a | All (u+g+o) |\n\n```\nchmod u+x script.sh     # Add execute for owner\nchmod go-w config.env   # Remove write from group and others\nchmod a+r readme.txt    # Add read for everyone\n```\n\n**2. Numeric (Octal) Mode** (commonly used in production):\n\nEach permission has a numeric value:\n\n| Permission | Value |\n|:---|:---|\n| Read (r) | 4 |\n| Write (w) | 2 |\n| Execute (x) | 1 |\n| None | 0 |\n\nAdd the values together for each group:\n\n| Octal | Symbolic | Meaning |\n|:---|:---|:---|\n| 7 | rwx | Read + Write + Execute |\n| 6 | rw- | Read + Write |\n| 5 | r-x | Read + Execute |\n| 4 | r-- | Read only |\n| 0 | --- | No permissions |\n\nCommon combinations:\n\n| Mode | Who | Common use |\n|:---|:---|:---|\n| 755 | rwxr-xr-x | Shell scripts, directories |\n| 644 | rw-r--r-- | Config files, web content |\n| 600 | rw------- | SSH private keys, secrets |\n| 700 | rwx------ | Private directories |",
      "commands": [
        {
          "name": "chmod +x",
          "purpose": "Makes a file executable (runnable as a script or program).",
          "syntax": "chmod +x filename",
          "parameters": "+x: adds execute permission. filename: script or binary file.",
          "example": "chmod +x deploy.sh",
          "expected_output": "(No output — permissions updated silently)",
          "output_explanation": "deploy.sh is now executable. You can run it with ./deploy.sh.",
          "when_to_use": "Use after creating a new shell script to make it runnable.",
          "real_world_use": "Every new script created in a CI/CD pipeline must be chmod +x before the runner can execute it."
        },
        {
          "name": "chmod 600",
          "purpose": "Sets owner-only read/write, no permissions for anyone else.",
          "syntax": "chmod 600 filename",
          "parameters": "600: 6=rw for owner, 0=none for group, 0=none for others.",
          "example": "chmod 600 ~/.ssh/id_rsa",
          "expected_output": "(No output — permissions updated silently)",
          "output_explanation": "The SSH private key is now protected. Only the owner can read or write it.",
          "when_to_use": "Always set SSH private keys and .env files to 600.",
          "real_world_use": "SSH client refuses to use a private key file unless its permissions are 600 or more restrictive."
        },
        {
          "name": "chmod 755",
          "purpose": "Sets owner full access, group and others read+execute only.",
          "syntax": "chmod 755 filename",
          "parameters": "755: 7=rwx for owner, 5=r-x for group, 5=r-x for others.",
          "example": "chmod 755 startup.sh",
          "expected_output": "(No output — permissions updated silently)",
          "output_explanation": "The script can be executed by anyone, but only the owner can modify it.",
          "when_to_use": "Use for public-facing scripts, web directories, and shared executable files.",
          "real_world_use": "Web server document root (/var/www/html) is typically set to 755 so nginx/apache can read and serve files."
        }
      ],
      "pro_tip": "Use `chmod -R 755 directory/` to apply permissions recursively to all files in a directory tree.",
      "best_practices": "Never use chmod 777 in production. It gives everyone full read, write, and execute access — a major security risk.",
      "common_mistakes": "Setting chmod 777 on everything to 'fix permission errors' — this is a security vulnerability, not a fix.",
      "real_world_example": "After uploading a deployment script to a server, a DevOps engineer runs `chmod 755 deploy.sh` and `chmod 600 .env` to ensure correct access levels before running the deployment.",
      "remember": "SSH will not use private keys with permissions looser than 600. It will display 'WARNING: UNPROTECTED PRIVATE KEY FILE!' and refuse to connect.",
      "summary": "chmod changes who can read, write, or execute a file. Use symbolic mode (u+x) for clarity and numeric mode (755) for production scripts."
    },
    {
      "title": "Changing Ownership with chown and chgrp",
      "definition": "chown changes the user and/or group that owns a file. chgrp changes only the group.",
      "why_important": [
        "Web servers run as www-data — web files must be owned by www-data to serve correctly.",
        "Applications running as specific users need to own their config and data files.",
        "Mismatched ownership prevents services from reading their own config files.",
        "Security policies require files to be owned by the user running the process."
      ],
      "explanation": "Every file in Linux has an owner user and an owner group.\n\nYou can see ownership with `ls -l`:\n\n  -rw-r--r-- 1 **student** **student** 45 Jan 12 config.env\n                 ↑owner      ↑group\n\nTo change ownership:\n```\nchown user file         # Change owner only\nchown user:group file   # Change owner AND group\nchgrp group file        # Change group only\n```\n\nCommon real-world ownership patterns:\n\n| Service | User | Files |\n|:---|:---|:---|\n| Nginx/Apache | www-data | /var/www/html/ |\n| MySQL | mysql | /var/lib/mysql/ |\n| Jenkins | jenkins | /var/jenkins_home/ |\n| SSH keys | username | ~/.ssh/ |",
      "commands": [
        {
          "name": "chown",
          "purpose": "Changes the owner (and optionally group) of a file.",
          "syntax": "chown user[:group] filename",
          "parameters": "user: new owner username. group: optional new group. filename: target file.",
          "example": "chown www-data:www-data /var/www/html/index.html",
          "expected_output": "(No output — ownership changed silently)",
          "output_explanation": "The web server (www-data) now owns index.html and can read/write it.",
          "when_to_use": "Use when deploying application files to ensure the service user owns them.",
          "real_world_use": "After deploying a PHP app to /var/www, run chown -R www-data:www-data /var/www/myapp to let Apache serve the files."
        },
        {
          "name": "chown -R",
          "purpose": "Recursively changes ownership of a directory and all its contents.",
          "syntax": "chown -R user:group directory/",
          "parameters": "-R: recursive. user:group: new owner and group. directory: target folder.",
          "example": "chown -R student:student ~/projects/",
          "expected_output": "(No output — all files updated silently)",
          "output_explanation": "Every file and subdirectory inside ~/projects/ now belongs to the student user.",
          "when_to_use": "Use after copying files as root — the copies inherit root ownership and must be reassigned.",
          "real_world_use": "After extracting an application archive as root, run chown -R appuser:appuser /opt/myapp to reassign ownership to the app service account."
        }
      ],
      "pro_tip": "You can use `chown user: file` (colon with no group) to change owner and also set group to the user's primary group.",
      "best_practices": "Run application processes as dedicated non-root service users (e.g., www-data, jenkins, postgres) and ensure files are owned by those users.",
      "common_mistakes": "Forgetting -R when changing ownership of a directory — only the directory itself changes, not its contents.",
      "real_world_example": "A Jenkins CI pipeline deploys a Java application to /opt/myapp. After deployment, the team runs `chown -R jenkins:jenkins /opt/myapp` to ensure the Jenkins service account can access all deployed files.",
      "remember": "Only root can change file ownership. Regular users can only change permissions on files they own.",
      "summary": "chown and chgrp assign files to the right users and groups. Critical for web servers, databases, and any service running as a non-root user."
    }
  ],
  "interactive_examples": [
    {
      "objective": "Check the current permissions of all files in your home directory.",
      "scenario": "Before a security audit, you need to verify that no sensitive files are world-readable.",
      "command": "ls -la ~",
      "explanation": "ls -la lists all files (including hidden) in long format, showing permissions, ownership, size, and modification date for every item in the home directory.",
      "expected_output": "total 32\ndrwxr-xr-x 4 student student 4096 Jan 12 .\ndrwxr-xr-x 5 root    root    4096 Jan 12 ..\n-rw------- 1 student student  220 Jan 12 .bash_history\n-rw-r--r-- 1 student student 3526 Jan 12 .bashrc\ndrwx------ 2 student student 4096 Jan 12 .ssh\n-rw-r--r-- 1 student student   45 Jan 12 welcome.txt",
      "output_explanation": ".ssh directory is drwx------ (700) — private. .bashrc is -rw-r--r-- (644) — readable by all but writable only by owner.",
      "real_world_use": "Run this at the start of a security audit to quickly spot any files with overly permissive access.",
      "common_mistakes": "Not checking the .ssh directory — SSH keys with wrong permissions cause login failures.",
      "tips": "Pay attention to the .ssh directory — it should always be 700, and keys inside should be 600."
    },
    {
      "objective": "Make a shell script executable.",
      "scenario": "You just created a deployment script but can't run it — it throws 'Permission denied'.",
      "command": "chmod +x deploy.sh && ls -l deploy.sh",
      "explanation": "chmod +x adds execute permission. The ls -l after confirms the change — look for x in the permission string.",
      "expected_output": "-rwxr-xr-x 1 student student 120 Jan 12 deploy.sh",
      "output_explanation": "The x bits are now set for owner, group, and others. The script can now be executed with ./deploy.sh.",
      "real_world_use": "Every shell script in a CI/CD pipeline must be chmod +x before it can be executed by the runner.",
      "common_mistakes": "Running chmod 777 deploy.sh — gives everyone write access to your script, which is a security risk.",
      "tips": "Use chmod 755 for scripts that should be executable by all but only modified by the owner."
    },
    {
      "objective": "Protect a sensitive config file from other users.",
      "scenario": "Your .env file contains a database password. You need to restrict access so only you can read it.",
      "command": "chmod 600 .env && ls -l .env",
      "explanation": "600 = rw------- = owner can read and write, group and others have no access at all.",
      "expected_output": "-rw------- 1 student student 42 Jan 12 .env",
      "output_explanation": "Only the student user can read or write .env. No one else has any access.",
      "real_world_use": "Set all .env files, private keys, and credential files to 600 to prevent unauthorized access.",
      "common_mistakes": "Leaving credential files at 644 — other users on the system can read your passwords.",
      "tips": "SSH private keys (id_rsa) must be 600 or stricter. SSH will refuse to use them otherwise."
    },
    {
      "objective": "Change ownership of files to the web server user.",
      "scenario": "You deployed a web app as root. Now nginx can't serve the files because it runs as www-data.",
      "command": "chown -R www-data:www-data /var/www/html/ && ls -la /var/www/html/",
      "explanation": "chown -R recursively changes all files in /var/www/html/ to be owned by www-data user and group. The web server process can now read and serve them.",
      "expected_output": "total 24\ndrwxr-xr-x 2 www-data www-data 4096 Jan 12 .\ndrwxr-xr-x 4 root     root     4096 Jan 12 ..\n-rw-r--r-- 1 www-data www-data 1234 Jan 12 index.html",
      "output_explanation": "index.html is now owned by www-data. The nginx process (running as www-data) can now read and serve it.",
      "real_world_use": "Always chown web files to www-data after deploying to an nginx or Apache server.",
      "common_mistakes": "Forgetting -R and only changing the directory itself, not its contents.",
      "tips": "After chown, also verify permissions with ls -la. Files should be 644, directories 755 for web serving."
    },
    {
      "objective": "Verify SSH key permissions before connecting to a remote server.",
      "scenario": "You just generated a new SSH key pair and need to ensure the private key has correct permissions.",
      "command": "chmod 600 ~/.ssh/id_rsa && chmod 700 ~/.ssh && ls -la ~/.ssh/",
      "explanation": "Private key must be 600 (owner read/write only). The .ssh directory must be 700 (owner access only). SSH client enforces these permissions and will fail otherwise.",
      "expected_output": "total 16\ndrwx------ 2 student student 4096 Jan 12 .\ndrwxr-xr-x 5 student student 4096 Jan 12 ..\n-rw------- 1 student student 1823 Jan 12 id_rsa\n-rw-r--r-- 1 student student  381 Jan 12 id_rsa.pub",
      "output_explanation": ".ssh is 700 (private), id_rsa is 600 (private key), id_rsa.pub is 644 (public key — shareable).",
      "real_world_use": "SSH permissions are enforced by the SSH client. Incorrectly permissioned keys will fail with 'WARNING: UNPROTECTED PRIVATE KEY FILE!'",
      "common_mistakes": "Making id_rsa.pub (public key) private is harmless but making id_rsa (private key) world-readable is a critical security risk.",
      "tips": "The public key (id_rsa.pub) can be 644 — it's meant to be shared. The private key (id_rsa) must be 600 or stricter."
    }
  ],
  "lessons": [
    {
      "id": 1,
      "title": "1. Read Permissions (ls -l)",
      "definition": "Linux stores permission info per file showing who can read, write, or execute.",
      "explanation": "The ls -l command shows permissions in the first column as 10 characters.",
      "instruction": "List files with permissions using ls -l in your home directory.",
      "example": "ls -la /etc",
      "expected": "student",
      "hint": "Type: ls -l",
      "solution": "ls -l",
      "difficulty": "Beginner",
      "estimated_duration": "4m"
    },
    {
      "id": 2,
      "title": "2. Create a Script File",
      "definition": "Shell scripts contain commands executed line by line by the bash interpreter.",
      "explanation": "Create a file with .sh extension. It starts as a regular file with no execute permission.",
      "instruction": "Create a file named deploy.sh using touch.",
      "example": "touch hello.sh",
      "expected": "deploy.sh",
      "hint": "Type: touch deploy.sh",
      "solution": "touch deploy.sh",
      "difficulty": "Beginner",
      "estimated_duration": "3m"
    },
    {
      "id": 3,
      "title": "3. Make Script Executable (chmod +x)",
      "definition": "The execute bit (x) allows a file to be run as a program or script.",
      "explanation": "chmod +x adds execute permission to owner, group, and others.",
      "instruction": "Make deploy.sh executable using chmod +x.",
      "example": "chmod +x myscript.sh",
      "expected": "x",
      "hint": "Type: chmod +x deploy.sh",
      "solution": "chmod +x deploy.sh",
      "difficulty": "Beginner",
      "estimated_duration": "4m"
    },
    {
      "id": 4,
      "title": "4. Restrict File Access (chmod 600)",
      "definition": "Numeric mode 600 means only the owner can read and write — no one else has access.",
      "explanation": "This is the required permission level for SSH private keys and .env secret files.",
      "instruction": "Create a file secrets.txt and set its permissions to 600.",
      "example": "chmod 600 private_key",
      "expected": "600",
      "hint": "Type: touch secrets.txt && chmod 600 secrets.txt",
      "solution": "touch secrets.txt && chmod 600 secrets.txt",
      "difficulty": "Beginner",
      "estimated_duration": "5m"
    },
    {
      "id": 5,
      "title": "5. Set Script Permissions (chmod 755)",
      "definition": "Mode 755 gives the owner full access (rwx) and group+others read and execute only.",
      "explanation": "This is the standard permission for executable scripts accessible to all users.",
      "instruction": "Set permissions on deploy.sh to 755.",
      "example": "chmod 755 app.sh",
      "expected": "755",
      "hint": "Type: chmod 755 deploy.sh",
      "solution": "chmod 755 deploy.sh",
      "difficulty": "Beginner",
      "estimated_duration": "4m"
    },
    {
      "id": 6,
      "title": "6. Remove Group Write (chmod go-w)",
      "definition": "Symbolic mode go-w removes write permission from group (g) and others (o).",
      "explanation": "Use go-w to ensure config files cannot be modified by other users.",
      "instruction": "Remove write permissions for group and others from secrets.txt.",
      "example": "chmod go-w myconfig.conf",
      "expected": "rw-r--r--",
      "hint": "Type: chmod go-w secrets.txt",
      "solution": "chmod go-w secrets.txt",
      "difficulty": "Intermediate",
      "estimated_duration": "5m"
    },
    {
      "id": 7,
      "title": "7. Check Ownership (ls -l)",
      "definition": "Every file has an owner user and group shown in ls -l output.",
      "explanation": "The 3rd and 4th columns of ls -l output show the owner user and group respectively.",
      "instruction": "List files showing ownership details.",
      "example": "ls -la /var/www",
      "expected": "student student",
      "hint": "Type: ls -l",
      "solution": "ls -l",
      "difficulty": "Beginner",
      "estimated_duration": "3m"
    },
    {
      "id": 8,
      "title": "8. Create a Protected Directory (chmod 700)",
      "definition": "Mode 700 gives the owner full access to a directory while blocking all other users.",
      "explanation": "This is required for the ~/.ssh/ directory to ensure SSH security.",
      "instruction": "Create a directory named private_config and set it to mode 700.",
      "example": "mkdir mykeys && chmod 700 mykeys",
      "expected": "700",
      "hint": "Type: mkdir private_config && chmod 700 private_config",
      "solution": "mkdir private_config && chmod 700 private_config",
      "difficulty": "Intermediate",
      "estimated_duration": "5m"
    }
  ],
  "exercises": [
    {
      "title": "Read the Permission String",
      "problem": "Run `ls -la /etc/passwd` and identify: is it a file or directory? Who is the owner? Can group members write to it?",
      "difficulty": "easy",
      "hint": "The first character is - (file) or d (directory). Column 3 is owner, column 4 is group. The 5th character in permissions is group write.",
      "expected_result": "It's a file (-), owned by root, and group cannot write to it.",
      "objective": "Practice reading and interpreting permission strings."
    },
    {
      "title": "Create and Protect a Secrets File",
      "problem": "Create a file called `db_password.txt`, write `SUPER_SECRET_PASSWORD` into it, then set its permissions to 600.",
      "difficulty": "easy",
      "hint": "touch, then echo with >, then chmod 600.",
      "expected_result": "ls -l shows -rw------- for db_password.txt.",
      "objective": "Practice file creation and applying restrictive permissions."
    },
    {
      "title": "Create a Public Script",
      "problem": "Create a script `backup.sh` and set permissions so everyone can execute it, but only you can modify it.",
      "difficulty": "easy",
      "hint": "Use chmod 755 backup.sh. This gives owner rwx (7), group r-x (5), others r-x (5).",
      "expected_result": "ls -l shows -rwxr-xr-x for backup.sh.",
      "objective": "Understand when to use 755 vs 700 for scripts."
    },
    {
      "title": "Fix Permission Denied",
      "problem": "You have a script `run.sh` but get 'Permission denied' when executing it. What is wrong and how do you fix it?",
      "difficulty": "medium",
      "hint": "Run ls -l run.sh. If you see no x bit, the script is not executable. Fix with chmod +x run.sh.",
      "expected_result": "After chmod +x, ./run.sh executes without permission denied.",
      "objective": "Diagnose and fix missing execute permissions — a very common real-world issue."
    },
    {
      "title": "Simulate SSH Key Security",
      "problem": "Create a directory `~/.ssh_test` and a file inside it called `id_test_rsa`. Apply correct SSH permissions: 700 for the directory, 600 for the private key file.",
      "difficulty": "medium",
      "hint": "mkdir ~/.ssh_test && touch ~/.ssh_test/id_test_rsa && chmod 700 ~/.ssh_test && chmod 600 ~/.ssh_test/id_test_rsa",
      "expected_result": "ls -la ~/.ssh_test shows drwx------ for directory and -rw------- for key file.",
      "objective": "Understand why SSH enforces strict permissions on keys and directories."
    },
    {
      "title": "Bulk Permission Reset",
      "problem": "Create a folder `webfiles/` with 3 html files inside. Set the directory to 755 and all files to 644.",
      "difficulty": "medium",
      "hint": "Use mkdir, touch three files, then `chmod 755 webfiles/` and `chmod 644 webfiles/*.html`.",
      "expected_result": "ls -la webfiles/ shows drwxr-xr-x for the folder and -rw-r--r-- for each html file.",
      "objective": "Practice setting permissions for a typical web content directory."
    },
    {
      "title": "Lock Down a Config Directory",
      "problem": "Create a directory `app_secrets/` with three files inside. Use chmod -R 600 to restrict ALL of them. Then check: does the directory itself have the right permissions to enter with cd?",
      "difficulty": "hard",
      "hint": "chmod -R 600 sets files to 600 but also sets the directory to 600 (no execute bit = can't cd into it). Fix the directory with chmod 700 app_secrets/.",
      "expected_result": "Files are 600, directory is 700. You can cd into it but others cannot.",
      "objective": "Understand why directories need the execute bit (x) to allow cd, unlike files."
    },
    {
      "title": "Diagnose a Web Server Permission Failure",
      "problem": "Simulate: create `/tmp/webroot/index.html` owned by root with mode 640. Why would nginx (running as www-data) fail to serve it? What chmod and chown commands fix it?",
      "difficulty": "hard",
      "hint": "640 means others have no read. www-data is neither owner nor group, so it falls into 'others'. Fix: chmod 644 and chown www-data:www-data.",
      "expected_result": "After chmod 644, the 'others' group (which includes www-data) can read the file.",
      "objective": "Reason through permission failures in a web server context."
    },
    {
      "title": "Write a Permission Audit Script",
      "problem": "Using echo and ls -l, create a mini security audit: check permissions on ~/.ssh (should be 700), ~/.bashrc (should be 644), and any .env file if it exists.",
      "difficulty": "challenge",
      "hint": "Use ls -la ~/.ssh, ls -l ~/.bashrc, and ls -l .env. You can combine with echo statements for labeling.",
      "expected_result": ".ssh is 700, .bashrc is 644. Report any mismatches.",
      "objective": "Simulate a real security audit process using basic Linux commands."
    },
    {
      "title": "SUID Bit Investigation",
      "problem": "Run `ls -la /usr/bin/passwd`. You will see an 's' in the permissions. Look up what the SUID bit means and explain why /usr/bin/passwd needs it.",
      "difficulty": "challenge",
      "hint": "The SUID bit (s instead of x for owner) means the program runs as the file owner (root) not the calling user. passwd needs root access to modify /etc/shadow.",
      "expected_result": "-rwsr-xr-x root root /usr/bin/passwd — the s means SUID.",
      "objective": "Explore advanced permission concepts used in system security."
    }
  ],
  "quiz": [
    {
      "question": "What does the permission string `-rwxr-xr--` represent?",
      "type": "MCQ",
      "options": [
        "A directory with full access for everyone",
        "A file where owner has rwx, group has r-x, others have r-- only",
        "A file where everyone has full rwx access",
        "A file where only the owner can read it"
      ],
      "answer": "A file where owner has rwx, group has r-x, others have r-- only",
      "explanation": "Reading left to right: - (file), rwx (owner), r-x (group), r-- (others).",
      "incorrect_explanations": {
        "A directory with full access for everyone": "Directories start with d. This starts with -, meaning it's a regular file.",
        "A file where everyone has full rwx access": "Others only have r-- (read only), not full rwx.",
        "A file where only the owner can read it": "Group has r-x and others have r-- — they can also read."
      }
    },
    {
      "question": "What numeric permission is required for an SSH private key file?",
      "type": "MCQ",
      "options": ["777", "755", "644", "600"],
      "answer": "600",
      "explanation": "SSH private keys must be 600 (rw-------). SSH client will refuse to use a key with looser permissions.",
      "incorrect_explanations": {
        "777": "777 gives everyone read/write/execute — critically insecure for private keys.",
        "755": "755 gives group and others execute+read access — still too open for a private key.",
        "644": "644 lets group and others read your private key — SSH will refuse this."
      }
    },
    {
      "question": "Which command makes a script executable?",
      "type": "MCQ",
      "options": ["chmod 600 script.sh", "chmod +x script.sh", "chmod -w script.sh", "chmod 400 script.sh"],
      "answer": "chmod +x script.sh",
      "explanation": "+x adds execute permission. Without it, trying to run the script gives 'Permission denied'.",
      "incorrect_explanations": {
        "chmod 600 script.sh": "600 = rw------- — no execute bit, still not runnable.",
        "chmod -w script.sh": "-w removes write permission — does nothing to add execute.",
        "chmod 400 script.sh": "400 = r-------- — read only, not executable."
      }
    },
    {
      "question": "What is 644 in symbolic notation?",
      "type": "MCQ",
      "options": ["rwxrwxrwx", "rw-r--r--", "rwxr-xr-x", "rw-------"],
      "answer": "rw-r--r--",
      "explanation": "6 = rw- (read+write), 4 = r-- (read only), 4 = r-- (read only). Owner can write, group and others can only read.",
      "incorrect_explanations": {
        "rwxrwxrwx": "That's 777 — full permissions for everyone.",
        "rwxr-xr-x": "That's 755 — common for directories and executable files.",
        "rw-------": "That's 600 — private access only."
      }
    },
    {
      "question": "You need group members to write to a shared directory, but others should have no access. What permission do you set?",
      "type": "Scenario",
      "options": ["chmod 775", "chmod 770", "chmod 744", "chmod 700"],
      "answer": "chmod 770",
      "explanation": "770 = rwxrwx--- — owner and group have full access, others have no access at all.",
      "incorrect_explanations": {
        "chmod 775": "775 = rwxrwxr-x — others can read and execute, not what we want.",
        "chmod 744": "744 = rwxr--r-- — group can only read, not write.",
        "chmod 700": "700 = rwx------ — only the owner has access. Group has no access."
      }
    },
    {
      "question": "Which command changes the owner of a file to 'deploy_user'?",
      "type": "MCQ",
      "options": ["chmod deploy_user file", "chown deploy_user file", "chgrp deploy_user file", "usermod deploy_user file"],
      "answer": "chown deploy_user file",
      "explanation": "chown (change owner) changes the file's owner. chgrp changes only the group.",
      "incorrect_explanations": {
        "chmod deploy_user file": "chmod changes permissions, not ownership.",
        "chgrp deploy_user file": "chgrp changes only the group, not the owner.",
        "usermod deploy_user file": "usermod modifies user accounts, not file ownership."
      }
    },
    {
      "question": "What does the first character of a permission string tell you?",
      "type": "MCQ",
      "options": ["The owner's read permission", "Whether the file is executable", "The file type (file, directory, symlink)", "The file's group"],
      "answer": "The file type (file, directory, symlink)",
      "explanation": "The first character is the file type: - = regular file, d = directory, l = symbolic link.",
      "incorrect_explanations": {
        "The owner's read permission": "Owner permissions start at position 2-4, not position 1.",
        "Whether the file is executable": "Execute permission is shown by x in positions 4, 7, or 10.",
        "The file's group": "The group is shown in the ls -l output as a separate column, not in the permission string."
      }
    },
    {
      "question": "A web server process running as 'www-data' cannot read /var/www/html/index.html. The file is owned by root with mode 640. What is the problem?",
      "type": "Scenario",
      "options": [
        "The file has no execute permission",
        "www-data is in the 'others' category and 640 gives others no read permission",
        "The web server needs chmod 777",
        "The file is a directory, not a regular file"
      ],
      "answer": "www-data is in the 'others' category and 640 gives others no read permission",
      "explanation": "640 = rw-r----- — root (owner) has rw, root group has r, others have nothing. www-data is not root and likely not in root's group, so it falls into 'others' with no access.",
      "incorrect_explanations": {
        "The file has no execute permission": "HTML files don't need execute permission to be served by a web server.",
        "The web server needs chmod 777": "Never use 777. The fix is chmod 644 or chown www-data:www-data.",
        "The file is a directory, not a regular file": "We are dealing with a file (index.html), not a directory."
      }
    },
    {
      "question": "What does `chmod -R 755 /var/www/html` do?",
      "type": "MCQ",
      "options": [
        "Changes permissions of /var/www/html only",
        "Removes permissions from all files",
        "Recursively sets 755 on the directory and all files inside it",
        "Changes the owner recursively"
      ],
      "answer": "Recursively sets 755 on the directory and all files inside it",
      "explanation": "-R means recursive — it applies the permission change to the directory and every file/subdirectory within it.",
      "incorrect_explanations": {
        "Changes permissions of /var/www/html only": "Without -R, chmod only affects the specified target. -R makes it recursive.",
        "Removes permissions from all files": "755 adds permissions (rwxr-xr-x), it does not remove them.",
        "Changes the owner recursively": "chown -R changes ownership. chmod changes permissions."
      }
    },
    {
      "question": "Output prediction: After `chmod 750 mydir`, what is the ls -l output for mydir?",
      "type": "Output Prediction",
      "options": ["drwxr-xr-x", "drwxr-x---", "drwx------", "drwxrwxr-x"],
      "answer": "drwxr-x---",
      "explanation": "750 = 7(rwx) owner, 5(r-x) group, 0(---) others. It's a directory so it starts with d.",
      "incorrect_explanations": {
        "drwxr-xr-x": "That's 755 — others have r-x access, but 750 gives others no access.",
        "drwx------": "That's 700 — group also has no access, but 750 gives group r-x.",
        "drwxrwxr-x": "That's 775 — group has write access, but 750 gives group only r-x."
      }
    },
    {
      "question": "Which directory permission is required for `cd mydir` to work?",
      "type": "MCQ",
      "options": ["Read (r)", "Write (w)", "Execute (x)", "Both read and write (rw)"],
      "answer": "Execute (x)",
      "explanation": "The execute bit on a directory means 'can enter'. Without x, cd gives 'Permission denied' even if you have read access.",
      "incorrect_explanations": {
        "Read (r)": "Read on a directory allows listing contents (ls), not entering (cd).",
        "Write (w)": "Write on a directory allows creating/deleting files inside — not entering.",
        "Both read and write (rw)": "You only need execute (x) to enter. Read is for listing contents."
      }
    },
    {
      "question": "What is the purpose of `chown -R www-data:www-data /var/www/myapp`?",
      "type": "Scenario",
      "options": [
        "Grant all users permission to read /var/www/myapp",
        "Delete all files in /var/www/myapp",
        "Recursively transfer ownership of all files to www-data user and group",
        "Change file permissions to 777 recursively"
      ],
      "answer": "Recursively transfer ownership of all files to www-data user and group",
      "explanation": "chown -R www-data:www-data changes the owner AND group of every file in the directory tree to www-data. This is done after deploying web app files so the web server can read them.",
      "incorrect_explanations": {
        "Grant all users permission to read /var/www/myapp": "chown changes ownership, not permissions. Use chmod for permissions.",
        "Delete all files in /var/www/myapp": "chown never deletes files. -R means recursive ownership change.",
        "Change file permissions to 777 recursively": "chmod -R changes permissions. chown -R changes ownership."
      }
    }
  ],
  "resources": {
    "summary": "Linux permissions control access through three levels (owner, group, others) and three types (read, write, execute). chmod changes permissions, chown changes ownership. Get permissions right and you prevent unauthorized access and deployment failures.",
    "links": [
      {
        "title": "Linux Journey — Permissions",
        "url": "https://linuxjourney.com/lesson/file-permissions",
        "description": "Interactive, beginner-friendly guide to Linux file permissions."
      },
      {
        "title": "chmod Calculator",
        "url": "https://chmod-calculator.com/",
        "description": "Visual tool to convert between numeric and symbolic permission modes."
      },
      {
        "title": "Linux man page — chmod",
        "url": "https://man7.org/linux/man-pages/man1/chmod.1.html",
        "description": "Official chmod documentation with all flags and options."
      },
      {
        "title": "SSH Key Permissions Guide",
        "url": "https://www.ssh.com/academy/ssh/permissions",
        "description": "Why SSH enforces strict permissions on keys and configuration files."
      },
      {
        "title": "Understanding Linux File Permissions",
        "url": "https://www.redhat.com/en/topics/linux/linux-permissions",
        "description": "Red Hat's comprehensive guide to Linux file permissions."
      }
    ],
    "cheat_sheet": "# Linux Permissions Cheat Sheet\n\n## Reading Permission Strings\n-rwxr-xr--\n│ │││ │││ └── Others: r-- = read only\n│ │││ └────── Group:  r-x = read + execute\n│ └────────── Owner:  rwx = full access\n└──────────── Type:   - = file, d = directory\n\n## chmod Symbolic Mode\nchmod u+x file          # Add execute for owner\nchmod go-w file         # Remove write from group+others\nchmod a+r file          # Add read for all\nchmod u=rwx,go=r file   # Set exact permissions\n\n## chmod Numeric Mode\nchmod 777 file          # rwxrwxrwx — NEVER in production\nchmod 755 file          # rwxr-xr-x — scripts, directories\nchmod 644 file          # rw-r--r-- — config files, web content\nchmod 600 file          # rw------- — private keys, .env files\nchmod 700 dir/          # rwx------ — private directories\n\n## Common Permission Scenarios\n.env files, private keys:     chmod 600\nShell scripts (public):       chmod 755\nShell scripts (private):      chmod 700\nWeb content files:            chmod 644\nWeb content directories:      chmod 755\nSSH directory (~/.ssh):       chmod 700\n\n## chown / chgrp\nchown user file           # Change owner\nchown user:group file     # Change owner and group\nchown -R user:group dir/  # Recursive ownership change\nchgrp group file          # Change group only\n\n## Octal Reference\n0 = ---   4 = r--\n1 = --x   5 = r-x\n2 = -w-   6 = rw-\n3 = -wx   7 = rwx",
    "commands_table": [
      { "name": "ls -l", "syntax": "ls -l [path]", "description": "Show files with permissions, owner, and size." },
      { "name": "chmod +x", "syntax": "chmod +x file", "description": "Add execute permission to a file." },
      { "name": "chmod NNN", "syntax": "chmod 755 file", "description": "Set permissions using numeric (octal) mode." },
      { "name": "chmod -R", "syntax": "chmod -R 755 dir/", "description": "Recursively apply permissions to directory." },
      { "name": "chown", "syntax": "chown user[:group] file", "description": "Change file owner and optionally group." },
      { "name": "chown -R", "syntax": "chown -R user:group dir/", "description": "Recursively change ownership of directory." },
      { "name": "chgrp", "syntax": "chgrp group file", "description": "Change only the group of a file." },
      { "name": "stat", "syntax": "stat file", "description": "Show detailed file info including numeric permissions." }
    ],
    "revision_notes": [
      "Permission string format: [type][owner][group][others] — 10 characters total.",
      "Numbers: r=4, w=2, x=1. Add them: rwx=7, rw-=6, r-x=5, r--=4.",
      "SSH private keys MUST be 600. SSH directories MUST be 700.",
      "Web server files: directories 755, files 644, owned by web server user (www-data).",
      "chmod -R applies recursively. Be careful — it changes both files AND directories."
    ],
    "best_practices": [
      "Never use 777 in production — it's a security vulnerability, not a fix.",
      "Set .env and credential files to 600 immediately after creation.",
      "Use chown -R to transfer web app files to the web server user after deployment.",
      "Apply the principle of least privilege — give only the permissions needed, nothing more."
    ],
    "beginner_mistakes": [
      "Using chmod 777 to 'fix' permission errors — this removes all security boundaries.",
      "Forgetting -R with chown/chmod on directories — only the directory itself changes.",
      "Setting execute bit on config files — configs don't need to be executable.",
      "Leaving .env files at 644 — they should be 600 if they contain secrets."
    ],
    "interview_questions": [
      {
        "question": "What permissions should an SSH private key have and why?",
        "answer": "600 (rw-------). Only the owner needs to read/write the key. SSH client enforces this and refuses to use keys with looser permissions to prevent other users from stealing credentials."
      },
      {
        "question": "What is the difference between chmod 755 and chmod 644?",
        "answer": "755 (rwxr-xr-x) includes execute permission — used for scripts and directories. 644 (rw-r--r--) has no execute — used for regular files like configs and web content."
      },
      {
        "question": "Why does a directory need the execute bit (x) to allow cd?",
        "answer": "On directories, execute means 'can traverse/enter'. Without x, cd gives Permission Denied. Read (r) only allows listing with ls, not entering."
      }
    ],
    "books": [
      {
        "title": "The Linux Command Line — William Shotts",
        "description": "Excellent coverage of permissions, users, and file ownership in chapters 9-10."
      },
      {
        "title": "Linux System Administration — Tom Adelstein",
        "description": "In-depth guide to user management, permissions, and security in production Linux systems."
      }
    ]
  }
}